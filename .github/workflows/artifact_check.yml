name: Download Artifact

on:
  workflow_run:
    workflows: ["Flutter CI"]
    types:
      - completed

jobs:
  download:
    runs-on: windows-latest

    steps:
      - name: Download Artifact
        uses: actions/download-artifact@v2
        with:
          name: Notes Bundle
          path: ./
